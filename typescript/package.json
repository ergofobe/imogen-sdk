{
  "name": "imogen-sdk-typescript",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test packages",
    "test:live": "bun test live",
    "typecheck": "tsc -p packages/shared && tsc -p packages/sdk && tsc -p live",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "verify": "bun run lint && bun run typecheck && bun run build && bun run test",
    "publish:all": "bun run --filter '*' npm-publish"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0",
    "@types/bun": "^1.2.20",
    "typescript": "^5.9.2"
  }
}
